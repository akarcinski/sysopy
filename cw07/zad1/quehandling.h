#ifndef QUEHANDLING_H
#define QUEHANDLING_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int queue_push(char * queue);
int queue_pop(char * queue);
int queue_empty(char * queue);
int queue_full(char * queue);

#endif
